spring:
  application:
    name: IRCTC-backend

  mail:
    host: smtp.gmail.com
    port: 587
    username: mdtameem555@gmail.com
    password: kedp rett rotx fzqo

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  datasource:
    url: jdbc:mysql://localhost:3306/backendx
    username: root
    password: login123$
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8085


management:
  endpoints:
    web:
      exposure:
        include: "*"

  endpoint:
    health:
      show-details: always

train:
  image:
    folder:
      path: uploads/